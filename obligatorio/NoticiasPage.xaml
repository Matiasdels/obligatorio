<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="obligatorio.NoticiasPage" 
             Title="Últimas Noticias"
             BackgroundColor="{StaticResource AppBackgroundDark}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="NewsDateLabel" TargetType="Label">
                <Setter Property="TextColor" Value="#FCD34D"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="Margin" Value="0,5,0,0"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Frame Grid.Row="0" Style="{StaticResource AppHeaderFrame}">
            <Label Text="📰 Últimas Noticias" 
                   Style="{StaticResource AppTitleLabel}"
                   FontSize="22"
                   TextColor="{StaticResource White}"

                   HorizontalOptions="Center"/>
            
        </Frame>


        <Frame Grid.Row="1" 
               Style="{StaticResource AppBaseFrame}"
               CornerRadius="20"
               Margin="20,15,20,10"
               Padding="15"
               IsVisible="{Binding IsBusy}">
            <ActivityIndicator IsRunning="{Binding IsBusy}" 
                             IsVisible="{Binding IsBusy}" 
                             Color="{StaticResource AppAccentColor}"
                             HeightRequest="40"
                             HorizontalOptions="Center"/>
        </Frame>

        <CollectionView Grid.Row="2" 
                       x:Name="NoticiasCollection"
                       ItemsSource="{Binding Noticias}"
                       Margin="0,5,0,0">

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Style="{StaticResource AppCardFrame}" Margin="15,10">
                        <StackLayout Padding="15">

                  
                            <Label Text="{Binding Title}" 
                                   Style="{StaticResource AppSubtitleLabel}"
                                   FontSize="16"
                                   Margin="0,0,0,8"/>

               
                            <Label Text="{Binding Description}" 
                                   MaxLines="3" 
                                   LineBreakMode="TailTruncation"
                                   Style="{StaticResource AppBodyLabel}"
                                   FontSize="13"
                                   VerticalOptions="Fill"
                                   Margin="0,0,0,8"/>

                      <!--ESTO AGREGA LA HORA Y FECHA PERO SIEMPRE MARCA LA MISMA QUE ES CUANDO SE ABRE NOTICIA, 
                            LO PODEMOS INTENTAR ARREGLAR O DEJAR SIN USAR PORQUE NO ES NECESARIO-->
                            <!--<Label Text="{Binding PubDate}" 
                                   Style="{StaticResource NewsDateLabel}"
                                   TextColor="{StaticResource PrimaryBlue}"
                                   HorizontalOptions="End"/>-->
                                   

                        </StackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>


            <CollectionView.EmptyView>
                <StackLayout VerticalOptions="Center" 
                           HorizontalOptions="Center"
                           Padding="40">
                    <Label Text="📰" 
                           FontSize="48" 
                           HorizontalOptions="Center"/>
                    <Label Text="No se encontraron noticias" 
                           Style="{StaticResource AppSubtitleLabel}"
                           FontSize="16"
                           HorizontalOptions="Center"
                           Margin="0,10,0,0"/>
                </StackLayout>
            </CollectionView.EmptyView>
        </CollectionView>

        <Button Grid.Row="3"
                x:Name="btnAtras" 
                Text="← Volver atrás" 
                Clicked="btnNoticiasAtras_Clicked"
                Style="{StaticResource AppPrimaryButton}"
                HeightRequest="45"
                Margin="20,10,20,15"/>
    </Grid>
</ContentPage>